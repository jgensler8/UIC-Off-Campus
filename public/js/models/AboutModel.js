define([
  'jquery',
  'underscore',
  'Backbone'
  ],
function( $, _, Backbone){
  var AboutModel = new Backbone.Model.extend({
    initialize: function(){
      
    }
  });

  return AboutModel;
})
